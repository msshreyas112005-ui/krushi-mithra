# Railway deployment configuration for KRISHI MITHRA

# Build settings
[build]
  builder = "NIXPACKS"
  buildCommand = "cd backend && npm install"
  startCommand = "cd backend && npm start"

# Environment variables (set these in Railway dashboard)
[env]
  NODE_ENV = "production"
